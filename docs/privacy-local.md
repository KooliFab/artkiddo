# Local privacy commitment

The public shell requires no account, secret, or network. Images, audio stories,
and metadata remain in the device `LocalVault`. Logs must not contain tokens,
artwork content, or complete local paths. Any remote extension must be explicit,
capability-gated, and documented in the local capability matrix.
