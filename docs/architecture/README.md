# Public architecture

Start with `current-state.md`. Open the others only when the task needs them:

- `repository-map.md`: entry points per area;
- `feature-matrix.md`: feature ownership by repository;
- `dependency-rules.md`: what a public dependency may be;
- `data-flows.md`: local creation, trash, rescue export and optional sync flows;
- `generated-inventory.md`: mechanical file index, regenerated by
  `tool/generate_inventory.dart`.
