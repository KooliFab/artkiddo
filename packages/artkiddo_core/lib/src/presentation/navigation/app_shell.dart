import 'package:flutter/material.dart';

import '../children/children_screen.dart';

class AppShell extends StatelessWidget {
  const AppShell({super.key});

  @override
  Widget build(BuildContext context) => const ChildrenScreen();
}
